# SiYuan & S3 MCP Server Project

This repository contains two Model Context Protocol (MCP) servers: one for interacting with SiYuan Note (`siyuan-mcp-server`) and one for S3-compatible storage (`s3-mcp-server`).

Your primary sources of information are the consolidated documentation files in the `docs/` directory. Please refer to them based on your task.

## Key Documentation Files

*   **`docs/CONFIGURATION.md`**: **Complete setup and configuration reference.** Contains installation instructions, environment variables, client configuration for Claude Desktop/Code/n8n, multi-tenant setup, and troubleshooting. Use this for all setup questions.

*   **`docs/USER_GUIDE.md`**: **Usage workflows and examples.** Contains core workflows, composite tool usage patterns, workflow examples, and best practices. Use this to understand how to *use* the tools effectively.

*   **`docs/DEPLOYMENT.md`**: **Production deployment guide.** Contains Docker deployment, reverse proxy setup (Caddy/Nginx), process management (PM2/systemd), multi-tenant architecture, monitoring, and security best practices. Use this for production deployments.

*   **`docs/DEVELOPER_GUIDE.md`**: **Contributions and architecture.** Contains project architecture, design rationale, contribution guidelines, coding standards, and development roadmap. Use this to understand how to *modify or extend* the project.

*   **`docs/TOOL_REFERENCE.md`**: **Complete API reference (49 tools).** Complete API-style reference for every tool, detailing exact parameters and return structure. Use this as a technical dictionary of capabilities.

### Additional Reference Files

*   **`docs/claude-code-subagents.md`**: Advanced subagent patterns and examples for organizing tools
*   **`docs/mcp-best-practices.md`**: Comprehensive MCP server development best practices
*   **`docs/siyuan_API.md`**: Official SiYuan API specification and endpoint reference

## Common Development Tasks

*   **Install dependencies:** `npm install`
*   **Run SiYuan server (dev mode):** `npm run dev`
*   **Run SiYuan server (v1 - legacy):** `npm run dev:v1`
*   **Run tests:** `npm test`
*   **Build for production:** `npm run build`
*   **Start compiled server:** `npm start`
*   **Clean build artifacts:** `npm run clean`

## Project Status

**Version**: 2.0.0
**Status**: ✅ Production Ready
**Tools**: 49 (43 atomic + 6 composite)
**Branch**: `v2-clean-atomic-tools`

## Environment Variables

*   **`SIYUAN_API_URL`**: Optional - Default: `http://localhost:6806` (SiYuan API endpoint)
*   **`PORT`**: Optional - Default: `3000` (MCP server port)

## Security Model

**🔐 CRITICAL: The SiYuan token MUST be sent by the client, NOT stored on the server!**

- Client sends `X-SiYuan-Token` header with each request
- Server extracts token and passes it to SiYuan API
- Server does NOT store tokens (stateless pass-through)

**Why?** The MCP server URL is public (accessible via HTTPS). Storing the token on the server would expose your private SiYuan notes to anyone who discovers the URL.

**Correct client configuration:**
```json
{
  "mcpServers": {
    "siyuan": {
      "url": "https://mcp.yourdomain.com/siyuan",
      "transport": "streamable-http",
      "headers": {
        "X-SiYuan-Token": "YOUR_PRIVATE_SIYUAN_TOKEN"
      }
    }
  }
}
```

## How to Proceed

1.  For any task, first determine if it's about *using*, *developing*, or *referencing* a specific tool.
2.  Open and read the corresponding guide in the `docs/` directory.
3.  Use the information within that guide to complete the task.

## Documentation Structure

All documentation is organized in the `docs/` directory:

```
docs/
├── CONFIGURATION.md           # Setup & configuration (all clients)
├── USER_GUIDE.md              # Usage workflows & examples
├── DEPLOYMENT.md              # Production deployment guide
├── DEVELOPER_GUIDE.md         # Architecture & contributing
├── TOOL_REFERENCE.md          # Complete API reference (49 tools)
├── claude-code-subagents.md   # Advanced subagent patterns
├── mcp-best-practices.md      # MCP development guidelines
└── siyuan_API.md              # SiYuan API specification
```

## Important Notes

- **v2.0 Implementation**: Located in `src-v2/` directory
- **v1.2.3 Reference**: Located in `src/` directory (legacy, kept for reference)
- **Compiled Output**: `dist/` directory (generated by `npm run build`)
- **Documentation**: All current documentation is in the `docs/` directory

---

*For detailed information about architecture, development workflow, or tool usage, please refer to the consolidated documentation files in the `docs/` directory.*
